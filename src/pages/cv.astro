---
import Base from '../layouts/Base.astro';
import Icon from '../components/Icon.astro';
import { site } from '../data/site';
import { education, experience, awards, service, skills, type Entry } from '../data/cv';
import { publications, kindLabels, type PubKind } from '../data/publications';
import { teaching } from '../data/teaching';

const order: PubKind[] = ['journal', 'conference', 'preprint', 'workshop', 'thesis'];
const grouped = order
  .map((kind) => ({ kind, items: publications.filter((p) => p.kind === kind) }))
  .filter((g) => g.items.length > 0);

const authorLine = (authors: string[]) => authors.join(', ');
---

<Base
  bandHeader
  title="Curriculum vitae"
  description={`Curriculum vitae of ${site.name}: education, research experience, publications, teaching, awards and academic service.`}
>
  <header class="cv-head band" id="hero">
    <div class="shell cv-head-inner">
      <div>
        <p class="label label--accent">Curriculum vitae</p>
        <h1>{site.name}</h1>
        <p class="muted role">{site.tagline} <span class="tick">/</span> {site.institution}</p>
        <p class="contact label">
          <a href={`mailto:${site.email}`}>{site.email}</a>
          <span class="tick" aria-hidden="true">/</span>
          <span>{site.location}</span>
          <span class="tick" aria-hidden="true">/</span>
          <a href={site.url}>{site.url.replace('https://', '')}</a>
        </p>
      </div>

      <div class="cv-actions no-print">
        <a class="btn" href={site.cvPdf} download>
          <Icon name="file" size={15} />
          PDF
        </a>
        <button class="btn" type="button" id="print-cv">Print</button>
      </div>
    </div>
  </header>

  <div class="shell cv">

    <section class="section">
      <h2>Education</h2>
      <ul class="entries">
        {
          education.map((entry: Entry) => (
            <li>
              <span class="period mono">{entry.period}</span>
              <div>
                <p class="entry-title">{entry.title}</p>
                <p class="muted org">
                  {entry.org}
                  {entry.location && <span class="faint"> — {entry.location}</span>}
                </p>
                {entry.detail?.map((line) => <p class="muted line">{line}</p>)}
              </div>
            </li>
          ))
        }
      </ul>
    </section>

    <section class="section">
      <h2>Research experience</h2>
      <ul class="entries">
        {
          experience.map((entry: Entry) => (
            <li>
              <span class="period mono">{entry.period}</span>
              <div>
                <p class="entry-title">{entry.title}</p>
                <p class="muted org">
                  {entry.org}
                  {entry.location && <span class="faint"> — {entry.location}</span>}
                </p>
                {entry.detail?.map((line) => <p class="muted line">{line}</p>)}
              </div>
            </li>
          ))
        }
      </ul>
    </section>

    <section class="section">
      <h2>Publications</h2>
      <p class="muted note">
        Authors are listed in contribution order; * marks equal contribution. The same list
        with PDFs, code and BibTeX is on the{' '}
        <a href="/publications/">publications page</a>.
      </p>
      {
        grouped.map((group) => (
          <div class="pub-group">
            <h3 class="label label--accent">{kindLabels[group.kind]}</h3>
            <ol class="pubs">
              {group.items.map((pub) => (
                <li>
                  <span class="pub-authors">{authorLine(pub.authors)}</span>.
                  <span class="pub-title"> {pub.title}</span>.
                  <span class="muted"> {pub.venueLong ?? pub.venue}, {pub.year}.</span>
                  {pub.note && <span class="pub-note"> {pub.note}.</span>}
                </li>
              ))}
            </ol>
          </div>
        ))
      }
    </section>

    <section class="section">
      <h2>Teaching</h2>
      <ul class="entries">
        {
          teaching.map((course) => (
            <li>
              <span class="period mono">{course.term}</span>
              <div>
                <p class="entry-title">
                  {course.code !== '—' && <span class="mono code">{course.code}</span>}
                  {course.title}
                </p>
                <p class="muted org">{course.role}, {course.institution}</p>
              </div>
            </li>
          ))
        }
      </ul>
    </section>

    <section class="section">
      <h2>Awards</h2>
      <ul class="entries tight">
        {
          awards.map((award) => (
            <li>
              <span class="period mono">{award.period}</span>
              <div>
                <p class="entry-title">{award.title}</p>
                <p class="muted org">{award.org}</p>
              </div>
            </li>
          ))
        }
      </ul>
    </section>

    <section class="section">
      <h2>Service</h2>
      <div class="service">
        <div>
          <h3 class="label label--accent">Reviewing</h3>
          <ul class="plain">{service.reviewing.map((s) => <li>{s}</li>)}</ul>
        </div>
        <div>
          <h3 class="label label--accent">Organising</h3>
          <ul class="plain">{service.organising.map((s) => <li>{s}</li>)}</ul>
        </div>
        <div>
          <h3 class="label label--accent">Outreach</h3>
          <ul class="plain">{service.outreach.map((s) => <li>{s}</li>)}</ul>
        </div>
      </div>
    </section>

    <section class="section">
      <h2>Technical</h2>
      <ul class="entries tight">
        {
          skills.map((row) => (
            <li>
              <span class="period">{row.group}</span>
              <div><p class="muted">{row.items}</p></div>
            </li>
          ))
        }
      </ul>
    </section>
  </div>
</Base>

<script>
  const wire = () => {
    const btn = document.getElementById('print-cv');
    if (!btn || btn.dataset.wired) return;
    btn.dataset.wired = 'true';
    btn.addEventListener('click', () => window.print());
  };
  wire();
  document.addEventListener('astro:page-load', wire);
</script>

<style>
  .cv-head {
    padding-top: clamp(7rem, 13vw, 9.5rem);
    padding-bottom: clamp(2.5rem, 5vw, 3.5rem);
  }

  .cv-head-inner {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .tick {
    color: var(--accent-on-band);
    padding-inline: 0.35em;
  }

  h1 {
    margin-top: 1rem;
    font-size: var(--t-h1);
    font-variation-settings: 'opsz' 60;
    letter-spacing: -0.04em;
  }

  .role {
    margin-top: 0.9rem;
    font-size: 1.05rem;
  }

  .contact {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.2rem;
    margin-top: 0.9rem;
    line-height: 1.9;
  }

  .contact a:hover {
    color: var(--accent-on-band);
  }

  .cv-actions {
    display: flex;
    gap: 0.5rem;
  }

  .cv {
    padding-top: clamp(2rem, 4vw, 3rem);
  }

  .section {
    padding-block: 1.9rem;
  }

  .section h2 {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--accent);
    padding-bottom: 0.7rem;
    margin-bottom: 1.3rem;
    border-bottom: 1px solid var(--ink);
  }

  .entries {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 1.15rem;
  }

  .entries.tight {
    gap: 0.6rem;
  }

  .entries li {
    display: grid;
    grid-template-columns: 10rem 1fr;
    gap: 1.25rem;
  }

  .period {
    font-size: 0.78rem;
    color: var(--ink-faint);
    padding-top: 0.15rem;
  }

  .entry-title {
    font-weight: 550;
    font-size: 0.95rem;
  }

  .code {
    color: var(--accent);
    font-size: 0.8rem;
    margin-right: 0.45rem;
  }

  .org,
  .line {
    font-size: 0.9rem;
    margin-top: 0.15rem;
  }

  .note {
    font-size: 0.87rem;
    max-width: 62ch;
    margin-bottom: 1.4rem;
  }

  .pub-group + .pub-group {
    margin-top: 1.5rem;
  }

  .pubs {
    margin: 0.6rem 0 0;
    padding-left: 1.3rem;
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
    font-size: 0.9rem;
  }

  .pubs li::marker {
    color: var(--ink-faint);
    font-size: 0.8em;
  }

  .pub-title {
    font-style: italic;
  }

  .pub-note {
    color: var(--accent);
    font-weight: 550;
  }

  .service {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.75rem;
  }

  .plain {
    list-style: none;
    padding: 0;
    margin: 0.7rem 0 0;
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    font-size: 0.88rem;
    color: var(--ink-soft);
  }

  @media print {
    .cv-head {
      padding-top: 0;
      padding-bottom: 0.75rem;
    }

    .cv {
      padding-top: 0;
    }

    h1 {
      margin-top: 0.4rem;
      font-size: 24pt;
    }
  }

  @media (max-width: 46rem) {
    .entries li {
      grid-template-columns: 1fr;
      gap: 0.2rem;
    }

    .service {
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }
  }
</style>
