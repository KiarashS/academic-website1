---
import Icon from './Icon.astro';
import { profileLinks } from '../data/site';

interface Props {
  variant?: 'chips' | 'plain';
}

const { variant = 'chips' } = Astro.props;
---

<ul class:list={['links', variant]}>
  {
    profileLinks.map((link) => (
      <li>
        <a
          href={link.href}
          rel={link.href.startsWith('http') ? 'me noopener' : undefined}
          target={link.href.startsWith('http') ? '_blank' : undefined}
        >
          <Icon name={link.icon} size={15} />
          <span>{link.label}</span>
        </a>
      </li>
    ))
  }
</ul>

<style>
  .links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  a {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-size: 0.83rem;
    font-weight: 450;
    text-decoration: none;
    color: var(--ink-soft);
    transition:
      color 0.15s var(--ease),
      border-color 0.15s var(--ease),
      background 0.15s var(--ease),
      transform 0.15s var(--ease);
  }

  .chips a {
    padding: 0.5rem 0.95rem 0.5rem 0.8rem;
    border: 1px solid var(--rule);
    border-radius: 999px;
    background: var(--paper-2);
  }

  .chips a:hover {
    color: #fff;
    border-color: var(--accent);
    background: var(--accent);
    transform: translateY(-2px);
  }

  /* On a dark band the chips invert. */
  :global(.band) .chips a {
    border-color: var(--band-rule);
    background: transparent;
    color: var(--band-soft);
  }

  :global(.band) .chips a:hover {
    color: #fff;
    background: var(--accent);
    border-color: var(--accent);
  }

  :global(.band) .plain a {
    color: var(--band-soft);
  }

  :global(.band) .plain a:hover {
    color: var(--accent-on-band);
  }

  .plain {
    flex-direction: column;
    gap: 0.4rem;
  }

  .plain a:hover {
    color: var(--accent);
  }

  a :global(svg) {
    flex: none;
    opacity: 0.75;
  }
</style>
